import React, { FC } from 'react';
import { TagCreator } from '../../containers/TagCreator';

export const TableSettings: FC<{}> = () => {
  return (
    <div>
      <TagCreator />
    </div>
  );
};
